name: chatgpt-disabled-apply-patch
run-name: chatgpt-disabled-apply-patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: echo "Temporary apply-patch workflow is disabled."
