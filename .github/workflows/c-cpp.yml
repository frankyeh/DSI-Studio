name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: self_hosted

    steps:
    - uses: actions/checkout@v2
    - name: test
      dir /w
